<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AuthTheme" parent="AppTheme" />

    <style name="AuthTheme.Container" parent="ConstraintLayout.Center">
        <item name="android:layout_width">@dimen/auth_container_width</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@color/transparent</item>
        <item name="android:orientation">vertical</item>
        <item name="android:padding">@dimen/auth_container_padding</item>
    </style>

    <style name="AuthTheme.EditText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:maxLines">1</item>
        <item name="android:ellipsize">end</item>
    </style>

    <style name="AuthTheme.Button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_weight">1</item>
    </style>

</resources>