import dependencies.Libraries

buildscript {
    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath Libraries.Core.gradle
        classpath Libraries.Core.kotlin_plugin
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}